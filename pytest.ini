[pytest]
addopts = -s
markers =
    unit: marks unit tests
    integration: marks integration tests
    slow: marks slow-running tests
    production: marks tests requiring production dataset or full matrix


